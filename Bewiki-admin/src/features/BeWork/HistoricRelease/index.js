import Container from "./containers/HistoricRelease";
import { reducers, sagas } from "./reduxSagas";

const HistoricReleaseTable = {
  Container,
};

export { reducers, sagas, HistoricReleaseTable };
