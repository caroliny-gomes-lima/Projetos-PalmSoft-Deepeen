import styled from "styled-components";

const Content = styled.div(({ theme }) => {
  return {
    display: "flex",
    width: "100%",
    height: "100%",
  };
});

const Styles = {
  Content,
};

export default Styles;
